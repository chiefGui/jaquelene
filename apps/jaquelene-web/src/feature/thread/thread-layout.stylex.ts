import * as stylex from "@stylexjs/stylex";

export const threadLayout = stylex.create({
  column: {
    marginInline: "auto",
    maxWidth: "42rem",
    width: "100%",
  },
  gutter: {
    paddingInline: "1.5rem",
  },
});
