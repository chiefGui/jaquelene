import * as stylex from "@stylexjs/stylex";

export const elevation = stylex.defineVars({
  large: "0 20px 25px -5px oklch(0 0 0 / 10%), 0 8px 10px -6px oklch(0 0 0 / 10%)",
  xLarge: "0 25px 50px -12px oklch(0 0 0 / 25%)",
});
