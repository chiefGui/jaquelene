import * as stylex from "@stylexjs/stylex";

export const shellLayout = stylex.defineVars({
  headerHeight: "2.75rem",
});

export const contentPaneLayout = stylex.defineVars({
  asideWidth: "20rem",
  headerInsetEnd: "0px",
});
