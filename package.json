{
  "name": "jaquelene",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build:ipc": "bun run --cwd packages/ipc build",
    "build:ipc:development": "bun run --cwd packages/ipc build:development",
    "db:generate": "bun run --cwd apps/jaquelene-desktop db:generate",
    "dev": "bun run build:ipc:development && vp dev",
    "dev:web": "bun run --cwd apps/jaquelene-web dev",
    "build": "bun run typecheck && vp build",
    "build:web": "bun run --cwd apps/jaquelene-web build",
    "test": "bun run build:ipc && vp test",
    "check": "vp check",
    "format": "vp fmt",
    "lint": "vp lint",
    "typecheck": "bun run build:ipc && tsc -b --pretty",
    "package": "bun run build && bun run --cwd apps/jaquelene-desktop package",
    "package:dir": "bun run build && bun run --cwd apps/jaquelene-desktop package:dir"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite-plus": "^0.3.0"
  },
  "packageManager": "bun@1.4.0",
  "trustedDependencies": [
    "electron"
  ]
}
