import * as stylex from "@stylexjs/stylex";

export const shellChrome = stylex.create({
  header: {
    alignItems: "center",
    display: "flex",
    flexShrink: 0,
    height: "2.75rem",
    minWidth: 0,
  },
});
