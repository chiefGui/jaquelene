module jaquelene

enum OpenRouterConfigurationState {
  Disconnected = "disconnected"
  Configured = "configured"
}

structure OpenRouterConfiguration {
  state: OpenRouterConfigurationState
  keyLabel?: string
}

enum OpenRouterConnectState {
  Connected = "connected"
  Rejected = "rejected"
  Unavailable = "unavailable"
}

structure OpenRouterConnectResult {
  state: OpenRouterConnectState
  keyLabel?: string
}

[RendererAPI]
[Validator=TrustedRenderer]
[ContextBridge]
interface OpenRouterConnection {
  getConfiguration() -> OpenRouterConfiguration
  connect(apiKey: string) -> OpenRouterConnectResult
  disconnect()
}
